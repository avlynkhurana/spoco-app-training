import 'package:flutter/material.dart';

class DishDetail extends StatelessWidget {
  const DishDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: const Text("Dish Details..")),
    );
  }
}